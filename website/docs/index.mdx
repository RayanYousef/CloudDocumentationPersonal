---
slug: /
title: Introduction
sidebar_position: 1
description: Welcome to this Docusaurus documentation platform with 3D model viewers, in-browser editing, and versioning.
type: index
okf_version: "0.1"
tags:
  - okf
  - docusaurus
  - template
timestamp: "2026-07-19T00:00:00+00:00"
---

import siteConfig from '@site/site.config';

import Link from '@docusaurus/Link';

import DocCardList from '@theme/DocCardList';

import {useDocsSidebar} from '@docusaurus/plugin-content-docs/client';

export const TopLevelCategoryCards = () => {
  const sidebar = useDocsSidebar();
  const items = (sidebar?.items ?? []).filter((item) => item.type === 'category');
  return <DocCardList items={items} />;
};

# Introduction

Welcome to this documentation site. This platform is built on [Docusaurus 3](https://docusaurus.io/) and ships with several custom features to make authoring and presenting documentation easier.

<p><Link className="button button--primary button--lg" to="/viz">Explore the knowledge graph</Link></p>

:::info Source on GitHub
This site is built from the <a href={`https://github.com/${siteConfig.organizationName}/${siteConfig.projectName}`}>GitHub repository</a> — browse the source, open issues, or fork it as a base for your own docs.
:::

## What is included

* **3D model viewers** — embed interactive glTF or FBX models directly in any `.mdx` page using the `ModelViewer` and `FbxViewer` components.
* **In-browser editor** — visit `/editor` to edit Markdown files directly in the browser without a local development environment.
* **Versioning** — the version dropdown in the navbar lets readers switch between the latest docs and frozen release snapshots.
* **No local setup required** — the `/editor` page also supports uploading images and 3D models and browsing existing repo assets, so non-technical editors can update content without cloning the repository or installing anything locally.

## Getting started

Browse the sections below to see the custom components in action and to learn how to add and edit pages.

<TopLevelCategoryCards />
